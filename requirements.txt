# Core dependencies
pydantic>=1.10.0
pyarrow>=10.0.0
happybase>=1.2.0 ## hbase client remove if not using HBase
hdfs>=2.7.0  ## hdfs client remove if not using HDFS
kafka-python>=2.2.15
fastavro>=1.0.0

# Utilities
python-dateutil>=2.8.0
pyyaml>=6.0.0
requests>=2.28.0

# Monitoring
prometheus-client>=0.15.0

# Async processing
concurrent-log-handler>=0.9.0

# iceberg
pyiceberg>=0.9.1

# cassandra
cassandra-driver>=3.29.2

# spark
pyspark==3.5.7